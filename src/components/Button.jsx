import React from 'react'

export default function Button({styles}) {
  return (
    <button className={`py-4 px-6 rounded-full my-4 bg-blue-gradient font-poppins font-medium text-[18px] text-primary outline-none`}>
      Get started
    </button>
  )
}
